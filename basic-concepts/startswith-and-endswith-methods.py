# The startswith method looks for characters at the beginning of a string object
# It returns a boolean value
# The result is negative even if the criteria is in the string but not at the beginning 
salutation = "Mr. Kermit the Frog"

print(salutation.startswith("Mr."))

print()

# The endswith method looks for characters at the beginning of a string object
# It returns a boolean value
# The result is negative even if the criteria is in the string but not at the end 

print(salutation.endswith("Frog"))
